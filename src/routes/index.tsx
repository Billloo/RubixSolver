import { createFileRoute, Link } from "@tanstack/react-router";
import { Boxes, Gauge, MousePointerClick, Route as RouteIcon } from "lucide-react";

import { CubeNet } from "@/components/CubeNet";
import { Button } from "@/components/ui/button";
import { applyAlg, solvedState } from "@/lib/cube/model";
import { PUZZLES } from "@/lib/cube/puzzles";

const title = "Rubik's Cube Solver – Free Online 2x2 & 3x3 Solver | RubikSolver";
const description = "Solve your 2x2 or 3x3 Rubik's Cube online with RubikSolver. Enter your cube's colors and get a step-by-step solution with clear move instructions and interactive cube diagrams.";

export const Route = createFileRoute("/")({ head: () => ({ meta: [{ title }, { name: "description", content: description }, { property: "og:title", content: title }, { property: "og:description", content: description }] }), component: Index });
const PREVIEW = applyAlg(solvedState(3), "R U R' U' F2 L D' B R2 U");
function Index() {
  return <div><section className="hero-glow"><div className="mx-auto grid w-full max-w-6xl items-center gap-10 px-4 py-12 sm:gap-12 sm:px-6 sm:py-20 lg:grid-cols-[1.05fr_1fr] lg:py-28"><div><span className="inline-flex items-center gap-2 rounded-full border border-border bg-surface px-3 py-1 text-xs uppercase tracking-[0.18em] text-primary"><Boxes className="size-3.5" /> Cube solving studio</span><h1 className="mt-6 text-4xl font-semibold leading-[1.08] sm:text-5xl lg:text-6xl">Rubik's Cube Solver</h1><p className="mt-3 text-xl font-medium text-primary sm:text-2xl">Type in your colors. Get the turns.</p><p className="mt-5 max-w-2xl text-base text-muted-foreground sm:text-lg">Solve your 2×2 or 3×3 Rubik's Cube online with RubikSolver. Enter your cube's colors and get a step-by-step solution with clear move instructions and interactive cube diagrams. Your cube state stays in your browser and is not uploaded.</p><div className="mt-8 flex flex-wrap gap-3"><Button size="lg" variant="secondary" asChild><Link to="/solver/$puzzle" params={{ puzzle: "3x3" }}>Solve a 3x3</Link></Button><Button size="lg" variant="secondary" asChild><Link to="/solver/$puzzle" params={{ puzzle: "2x2" }}>Solve a 2x2</Link></Button></div></div><div className="panel flex justify-center overflow-hidden p-4 sm:p-6"><CubeNet state={PREVIEW} /></div></div></section><section className="mx-auto w-full max-w-6xl px-4 pb-6 sm:px-6"><h2 className="text-2xl font-semibold">Pick your puzzle</h2><p className="mt-2 max-w-2xl text-sm text-muted-foreground">Choose a 2×2 or 3×3 puzzle and follow a generated solution one move at a time.</p><div className="mt-6 grid gap-4 sm:grid-cols-2">{PUZZLES.filter((p) => p.slug === "2x2" || p.slug === "3x3").map((p) => <Link key={p.slug} to="/solver/$puzzle" params={{ puzzle: p.slug }} className="panel group flex flex-col gap-3 p-5 transition hover:-translate-y-1 hover:border-primary"><div className="flex items-center justify-between"><span className="font-display text-3xl font-semibold">{p.slug}</span><span className="rounded-full bg-primary/15 px-2.5 py-1 text-xs text-primary">Auto solver</span></div><div><p className="font-medium">{p.name}</p><p className="mt-1 text-sm text-muted-foreground">{p.tagline}</p></div><p className="mt-auto pt-2 text-xs uppercase tracking-wider text-muted-foreground">{p.pieces}</p></Link>)}</div></section><section className="mx-auto w-full max-w-6xl px-4 py-14 sm:px-6 sm:py-20"><h2 className="text-2xl font-semibold">Three steps, start to finish</h2><div className="mt-6 grid gap-4 md:grid-cols-3">{[{ icon: MousePointerClick, title: "Enter the colors", body: "Click stickers on the flat map of your cube. Centers stay locked so the orientation always matches the instructions." }, { icon: Gauge, title: "Let the engine search", body: "The 2×2 and 3×3 solving engines run in your browser, so your cube state stays on your device." }, { icon: RouteIcon, title: "Turn along", body: "Step through the solution move by move with plain-English descriptions and live cube diagrams. New to move symbols? Read the Rubik's Cube notation guide." }].map((f) => <div key={f.title} className="panel p-6"><f.icon className="size-5 text-primary" /><h3 className="mt-4 text-lg font-semibold">{f.title}</h3><p className="mt-2 text-sm text-muted-foreground">{f.body}</p></div>)}</div></section></div>;
}
