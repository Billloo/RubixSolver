import { createFileRoute, Link } from "@tanstack/react-router";
import { ArrowLeft, Clock3, RotateCcw, Trophy, Trash2 } from "lucide-react";
import { useCallback, useEffect, useMemo, useRef, useState } from "react";

export const Route = createFileRoute("/timer")({
  head: () => ({ meta: [{ title: "Rubik Cube Timer — RubikSolver" }, { name: "description", content: "A modern Rubik cube timer and speedtimer for 2x2 and 3x3 practice, with a built-in Rubik cube scrambler, solve history, and averages." }] }),
  component: TimerPage,
});

type Solve = { time: number; scramble: string };
const faces = ["R", "L", "U", "D", "F", "B"] as const;
const suffixes = ["", "'", "2"] as const;
const makeScramble = () => { const out: string[] = []; let last = ""; while (out.length < 20) { const face = faces[Math.floor(Math.random() * faces.length)]!; if (face === last) continue; out.push(face + suffixes[Math.floor(Math.random() * suffixes.length)]!); last = face; } return out.join(" "); };
const formatTime = (ms: number) => { const s = ms / 1000; return s < 60 ? s.toFixed(2) : `${Math.floor(s / 60)}:${(s % 60).toFixed(2).padStart(5, "0")}`; };

function TimerPage() {
  const [running, setRunning] = useState(false); const [elapsed, setElapsed] = useState(0); const [solves, setSolves] = useState<Solve[]>([]); const [scramble, setScramble] = useState(makeScramble); const started = useRef(0); const raf = useRef<number | null>(null);
  const tick = useCallback(() => { if (!started.current) return; setElapsed(performance.now() - started.current); raf.current = requestAnimationFrame(tick); }, []);
  const stop = useCallback(() => { if (!started.current) return; const time = performance.now() - started.current; started.current = 0; if (raf.current) cancelAnimationFrame(raf.current); setElapsed(time); setRunning(false); setSolves((s) => [{ time, scramble }, ...s].slice(0, 50)); setScramble(makeScramble()); }, [scramble]);
  const start = useCallback(() => { started.current = performance.now(); setElapsed(0); setRunning(true); raf.current = requestAnimationFrame(tick); }, [tick]);
  useEffect(() => () => { if (raf.current) cancelAnimationFrame(raf.current); }, []);
  useEffect(() => { const key = (e: KeyboardEvent) => { if (e.code !== "Space" || e.repeat) return; e.preventDefault(); running ? stop() : start(); }; window.addEventListener("keydown", key); return () => window.removeEventListener("keydown", key); }, [running, start, stop]);
  const stats = useMemo(() => { const t = solves.map((s) => s.time); const avg = t.length ? t.reduce((a, b) => a + b, 0) / t.length : null; const best = t.length ? Math.min(...t) : null; const worst = t.length ? Math.max(...t) : null; const five = t.slice(0, 5); const ao5 = five.length === 5 ? [...five].sort((a, b) => a - b).slice(1, 4).reduce((a, b) => a + b, 0) / 3 : null; return { avg, best, worst, ao5 }; }, [solves]);
  return <div className="mx-auto w-full max-w-6xl px-4 py-10 sm:px-6 sm:py-14"><div className="flex items-center justify-between"><Link to="/" className="inline-flex items-center gap-2 text-sm text-muted-foreground hover:text-foreground"><ArrowLeft className="size-4" /> Home</Link><span className="inline-flex items-center gap-2 rounded-full border border-border bg-surface px-3 py-1 text-xs uppercase tracking-[0.16em] text-primary"><Clock3 className="size-3.5" /> Speed Timer</span></div><header className="mt-8"><p className="text-sm font-medium uppercase tracking-[0.2em] text-primary">Train your solves</p><h1 className="mt-2 text-4xl font-semibold sm:text-5xl">RubikSolver Timer</h1><p className="mt-3 max-w-2xl text-muted-foreground">A clean, distraction-free Rubik cube timer and speedtimer for 2x2 and 3x3 practice. Press <kbd className="rounded border border-border bg-surface px-1.5 py-0.5 font-mono text-xs">Space</kbd> or tap the timer to start and stop.</p></header><section className="mt-8 panel overflow-hidden"><div className="border-b border-border px-6 py-5 sm:px-8"><div className="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"><div><p className="text-xs uppercase tracking-[0.16em] text-muted-foreground">Rubik cube scrambler</p><p className="mt-1 font-mono text-base sm:text-lg">{scramble}</p></div><button onClick={() => setScramble(makeScramble())} className="inline-flex items-center gap-2 self-start rounded-lg border border-border px-3 py-2 text-sm text-muted-foreground hover:bg-accent hover:text-foreground"><RotateCcw className="size-4" /> New scramble</button></div></div><button onClick={() => running ? stop() : start()} className="group flex min-h-[360px] w-full flex-col items-center justify-center bg-gradient-to-b from-background to-surface-2 px-6 text-center focus:outline-none focus:ring-2 focus:ring-primary/50"><span className="text-sm font-medium uppercase tracking-[0.25em] text-muted-foreground">{running ? "Solving" : "Ready"}</span><span className="mt-3 font-display text-7xl font-semibold tabular-nums tracking-tight sm:text-8xl">{formatTime(elapsed)}</span><span className="mt-5 rounded-full border border-border bg-background/80 px-4 py-2 text-sm text-muted-foreground group-hover:border-primary/40">{running ? "Press Space to stop" : "Press Space to start"}</span></button></section><section className="mt-8 grid gap-4 sm:grid-cols-4">{[["Average", stats.avg], ["Best", stats.best], ["Worst", stats.worst], ["Ao5", stats.ao5]].map(([label, value]) => <div key={label as string} className="panel p-5"><p className="text-xs uppercase tracking-[0.16em] text-muted-foreground">{label as string}</p><p className="mt-2 font-display text-2xl font-semibold tabular-nums">{typeof value === "number" ? formatTime(value) : "—"}</p></div>)}</section><section className="mt-8 panel overflow-hidden"><div className="flex items-center justify-between border-b border-border px-6 py-5"><div><h2 className="font-semibold">Solve history</h2><p className="mt-1 text-sm text-muted-foreground">Your last 50 solves are kept in this session.</p></div><button disabled={!solves.length} onClick={() => setSolves([])} className="inline-flex items-center gap-2 rounded-lg border border-border px-3 py-2 text-sm text-muted-foreground hover:bg-accent disabled:opacity-40"><Trash2 className="size-4" /> Clear</button></div>{solves.length ? <div className="divide-y divide-border">{solves.map((solve, i) => <div key={`${solve.time}-${i}`} className="grid grid-cols-[3rem_1fr_auto] items-center gap-3 px-6 py-4"><span className="text-sm text-muted-foreground">#{solves.length - i}</span><span className="truncate font-mono text-sm text-muted-foreground">{solve.scramble}</span><span className="font-display text-xl font-semibold tabular-nums">{formatTime(solve.time)}</span></div>)}</div> : <div className="flex flex-col items-center justify-center px-6 py-16 text-center"><Trophy className="size-8 text-primary" /><p className="mt-3 font-medium">No solves yet</p><p className="mt-1 text-sm text-muted-foreground">Your times will appear here after your first solve.</p></div>}</section></div>;
}
